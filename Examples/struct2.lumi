struct Person {
    age: int;
    name: str;
}

let person: Person -> new Person(1, "test");
person.age = 5;
print person.age;

if (person.name == "test") {
    let b -> person.age;
    print b * 7.25;
}