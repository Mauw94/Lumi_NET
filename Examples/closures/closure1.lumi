struct Counter {
    value: int

    fn makePrinter() {
        fn printer() {
            print this.value;
        }
        return printer;
    }
}

let counter -> new Counter(value: 5);
let printer -> counter.makePrinter();
printer(); // Prints closure