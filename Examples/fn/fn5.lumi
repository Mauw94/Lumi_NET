struct Counter {
    value: int;

    fn increment(delta) {
        this.value = this.value + delta;
    }
}

let counter: Counter -> new Counter(0);

for i in 1 to 10 {
    counter.increment(3);    
}

print counter.value;